apiVersion: v1
kind: Service
metadata:
  name: {{ include "keystone.fullname" . }}-backend
  labels:
    {{- include "keystone.labels" . | nindent 4 }}
    {{- include "keystone.backend.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keystone.backend.selectorLabels" . | nindent 4 }}
