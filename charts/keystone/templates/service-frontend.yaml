apiVersion: v1
kind: Service
metadata:
  name: {{ include "keystone.fullname" . }}-frontend
  labels:
    {{- include "keystone.labels" . | nindent 4 }}
    {{- include "keystone.frontend.selectorLabels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.frontend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "keystone.frontend.selectorLabels" . | nindent 4 }}
